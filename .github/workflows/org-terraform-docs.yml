name: Org Terraform Docs

permissions:
  contents: read
  pull-requests: write

on:
  pull_request:
    paths:
      - "**.tf"
      - "**.tfvars"
      - "**.hcl"
      - ".terraform.lock.hcl"
      - "**/terraform/**"
      # README.md is generated from these, so a prose-only or config-only
      # edit must run the check too. Without them a stale README ships green.
      - "_header.md"
      - "_footer.md"
      - ".terraform-docs.yml"
  workflow_call:

jobs:
  terraform-docs:
    uses: Coalfire-CF/Actions/.github/workflows/org-terraform-docs.yml@162d71ff9bace6c2994b3b8d9e5bfc33f6b54eed # v0.18.0
